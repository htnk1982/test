name: PDRM Relative Spectral Boundary LAB
on:
  push:
    branches: [pdrm-note-sub-lab-v1]
    paths:
      - 'PDRM_Local_Render_Engine_v1/relative_spectral_v38.py'
      - 'PDRM_Local_Render_Engine_v1/role_observer_v38.py'
      - 'PDRM_Local_Render_Engine_v1/tests/test_relative38.py'
      - '.github/workflows/pdrm-boundary38-lab.yml'
permissions:
  contents: read
jobs:
  contracts:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    defaults:
      run:
        working-directory: PDRM_Local_Render_Engine_v1
    env:
      OMP_NUM_THREADS: '1'
      OPENBLAS_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install numpy scipy soundfile pyloudnorm
      - name: Test 86 previous and 34 new contracts; preserve production core
        shell: python
        run: |
          from pathlib import Path
          import sys,subprocess,unittest,json,platform,importlib.metadata
          sys.path.insert(0,str(Path.cwd()))
          base='6509e836984425259b6ee631674c5075486c7f77'
          frozen=['distribution_finish.py','hf_temporal_contrast_lab.py','offline_peak_lab.py','offline_peak_stream_v34.py','auto_peak_v34.py','natural_finish_v34.py','processed_finish.py']
          for name in frozen:
              before=subprocess.check_output(['git','show',base+':PDRM_Local_Render_Engine_v1/'+name])
              assert before.replace(b'\r\n',b'\n')==Path(name).read_bytes().replace(b'\r\n',b'\n'),name
          suite=unittest.TestSuite()
          for pattern in ('test_lowend_boundary_lab.py','test_context_observer_lab.py','test_boundary37.py','test_relative38.py'):
              suite.addTests(unittest.defaultTestLoader.discover('tests',pattern=pattern))
          result=unittest.TextTestRunner(verbosity=2).run(suite)
          report=dict(tests=result.testsRun,ok=result.wasSuccessful(),failures=len(result.failures),errors=len(result.errors),skipped=len(result.skipped),platform=platform.platform(),python=sys.version,versions={n:importlib.metadata.version(n) for n in ('numpy','scipy','soundfile','pyloudnorm')},frozen_core='MATCH',scope='Synthetic contracts only. No neural inference, private audio/reviews, listening test or EXE build in CI.')
          Path('BOUNDARY38_TEST_RESULTS.json').write_text(json.dumps(report,indent=2),encoding='utf-8')
          assert result.testsRun==120 and result.wasSuccessful() and not result.skipped
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: boundary38-contracts-${{ matrix.os }}
          path: PDRM_Local_Render_Engine_v1/BOUNDARY38_TEST_RESULTS.json
          retention-days: 30
